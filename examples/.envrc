# Project-local environment variables
# Place this file as .envrc in your project root
# Run `direnv allow` once to trust it
# Variables auto-load on cd, auto-unload on leaving

export AWS_PROFILE=my-aws-profile
export KUBECONFIG=~/.kube/my-cluster
export GOOGLE_CLOUD_PROJECT=my-gcp-project
export ENVIRONMENT=dev

# Database
export DB_HOST=localhost
export DB_PORT=5432
export DB_NAME=mydb
export DB_USER=dbuser
export DB_PASSWORD=localpassword

# Optional: add project bin to PATH
# PATH_add bin
# PATH_add ./node_modules/.bin

# Optional: load .env file (if you also use .env)
# dotenv
